let myEmail = 'gabriel@gmail.com'
let shoppingCart = ['Ovos', 'Toddy', 'Vinagre']

const goShopping = () => {
  return shoppingCart
}

let pastel = 'yes'
let megasenaNumbers = undefined
let something = null

const checkSkyColor = (skyColor) => skyColor === 'blue' ? true : false

let array = [1, 2, 42]

module.exports = {
  myEmail,
  goShopping,
  pastel,
  megasenaNumbers,
  something,
  checkSkyColor,
  array
}